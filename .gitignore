# macOS metadata
.DS_Store

# Audio is NEVER committed (large + may be copyrighted streaming downloads).
# Blanket-ignore every audio container anywhere in the tree:
*.mp3
*.wav
*.m4a
*.flac
*.ogg
*.aac

# Dataset / download / cache directories (incl. post-reorg renamed names)
songs/
standardized_songs_30s/
new_ai_tracks_120s/
new_ai_tracks_30s/
new_ai_tracks_10s/
ai_tracks_120s/
human_tracks_120s/
human_tracks_10s/
url_test_downloads/

# Cached embeddings / model weights / intermediate binaries
*.npz
*.ckpt
*.pt
*.pth

# Multi-codec data-augmentation artifacts (large; regenerate via
# augment_training_data.py + train_augmented_classifier.py — do not push)
.clap_train_embeddings_augmented.npz
augmented_validation_results.csv
03_Execution_Scripts/data/train_augmented/

# Runtime logs (verbose stderr dumps)
*.log

# Large model checkpoints — download via benchmark_runner.py
checkpoints/

# HuggingFace download cache
.hf_audio_cache/
.hf_sonics_meta/
.hf_aime_cache/
.fma_meta/

# Cloned detector repos (reinstalled via benchmark_runner.py)
FST-AI-Music-Detection/
ai-music-detection/

# Python cache
__pycache__/
*.pyc
.venv/
